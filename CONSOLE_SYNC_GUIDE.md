# 控制台文本与算子工作流同步功能

## 功能概述

这个功能允许算子工作流监听控制台文本的执行进度，当控制台显示某个算子执行完成时，工作流中对应的算子状态会自动同步更新。

## 工作原理

### 1. 控制台消息监听
- 工作流执行时会监听 `consoleMessages` 数组的变化
- 当检测到消息格式为 `"算子 {算子名称} 执行完成! 耗时: {时间}"` 时触发状态更新

### 2. 算子状态同步
- 使用正则表达式 `/算子 (.+?) 执行完成!/` 提取算子名称
- 在工作流步骤中查找匹配的算子名称
- 将对应步骤标记为已完成 (`isCompleted = true`)
- 更新当前执行步骤索引

### 3. 完成检测
- 当检测到消息包含 "任务流程执行完成" 时，自动结束整个工作流
- 当所有步骤都标记为完成时，也会自动结束工作流

## 实现细节

### 核心文件修改

1. **useWorkflow.ts**
   - 添加了 `setupConsoleBasedExecution()` 方法用于监听控制台消息
   - 添加了 `checkOperatorCompletion()` 方法用于检查算子完成状态
   - 修改了 `executeWorkflow()` 方法支持可选的控制台消息参数

2. **DataPreparation.vue**
   - 添加了 `consoleMessages` props 接收控制台消息
   - 修改了 `executeWorkflow()` 方法传递控制台消息给工作流钩子

3. **index.vue**
   - 在 `DataPreparation` 组件上添加了 `:consoleMessages="consoleMessages"` 属性传递

### 使用方式

1. **手动执行（默认模式）**
   ```javascript
   executeWorkflow(); // 不传参数，使用原有的模拟执行
   ```

2. **控制台同步模式**
   ```javascript
   executeWorkflow(consoleMessages); // 传递控制台消息，启用同步模式
   ```

## 测试验证

### 开发环境测试步骤

1. 打开开发者工具控制台
2. 进入数据制备页面
3. 添加一些算子到工作流中
4. 点击"执行工作流"按钮
5. 观察控制台输出，应该看到：
   ```
   开始监听控制台消息同步算子执行状态
   当前控制台消息数量: X
   算子 {算子名称} 已完成，当前步骤: X
   ```

### 验证控制台消息格式

确保控制台消息中包含以下格式的文本：
```
算子 数据缺失值填充算子 执行完成! 耗时: 0.02秒
算子 领域知识迁移算子 执行完成! 耗时: 44.54秒
...
任务流程执行完成, 耗时: 1485.44秒
```

## 注意事项

1. **算子名称匹配**：工作流中的算子名称必须与控制台消息中的算子名称完全一致
2. **消息顺序**：控制台消息的顺序应该与工作流中算子的执行顺序对应
3. **错误处理**：如果控制台消息格式不正确或算子名称不匹配，会在控制台输出相关调试信息

## 兼容性

- 新功能保持向后兼容，不传递控制台消息参数时使用原有的模拟执行模式
- 可以在运行时动态选择执行模式，不影响现有功能

## 调试模式

在开发环境中，会在控制台输出详细的调试信息：
- 监听开始提示
- 当前控制台消息数量
- 算子完成状态更新日志
- 工作流执行进度信息
